# 🚀 دليل النشر على GitHub Pages

دليل خطوة بخطوة لنشر محلل التداول على GitHub Pages

---

## ✅ المتطلبات الأساسية

قبل البدء، تأكد من:

- [x] لديك حساب GitHub
- [x] Git مثبت على جهازك
- [x] Node.js مثبت (الإصدار 18 أو أحدث)
- [x] المشروع يعمل محلياً بنجاح

---

## 📝 الخطوة 1: تحديث إعدادات المشروع

### 1.1 تحديث vite.config.js

افتح ملف `vite.config.js` وحدّث السطر التالي:

```javascript
base: '/trading-analyzer-pwa/',  // ✅ اسم المستودع الخاص بك
```

إذا كان اسم مستودعك مختلف، استبدل `trading-analyzer-pwa` باسم مستودعك الفعلي.

### 1.2 تحديث package.json (اختياري)

تأكد من وجود سكريبت deploy:

```json
"scripts": {
  "deploy": "npm run build && gh-pages -d dist"
}
```

---

## 📦 الخطوة 2: إنشاء مستودع GitHub

### 2.1 إنشاء مستودع جديد

1. اذهب إلى: https://github.com/new
2. **Repository name**: `trading-analyzer-pwa`
3. **Description**: محلل تداول متقدم - PWA
4. اختر **Public** (مهم لـ GitHub Pages المجاني)
5. **لا تختر** "Add a README file"
6. اضغط **Create repository**

### 2.2 نسخ رابط المستودع

انسخ الرابط الذي سيظهر، مثل:
```
https://github.com/YOUR_USERNAME/trading-analyzer-pwa.git
```

---

## 💻 الخطوة 3: رفع الكود إلى GitHub

### 3.1 تهيئة Git

افتح Terminal/CMD في مجلد المشروع:

```bash
# تهيئة مستودع git محلي
git init

# إضافة جميع الملفات
git add .

# Commit أول
git commit -m "Initial commit: Trading Analyzer PWA v1.0"

# تحديد الفرع الرئيسي
git branch -M main
```

### 3.2 ربط المستودع المحلي بـ GitHub

```bash
# استبدل YOUR_USERNAME باسم المستخدم الخاص بك
git remote add origin https://github.com/YOUR_USERNAME/trading-analyzer-pwa.git

# رفع الكود
git push -u origin main
```

**ملاحظة**: قد يطلب منك GitHub اسم المستخدم وكلمة المرور (أو Personal Access Token).

---

## ⚙️ الخطوة 4: تفعيل GitHub Pages

### 4.1 الذهاب لإعدادات المستودع

1. اذهب إلى صفحة المستودع على GitHub
2. اضغط على **Settings** (في الأعلى)
3. من القائمة الجانبية، اختر **Pages**

### 4.2 تفعيل GitHub Actions

في قسم "Build and deployment":

1. **Source**: اختر **GitHub Actions**
2. سيظهر لك إشعار بأن Workflow سيبدأ تلقائياً

### 4.3 انتظار البناء

1. اذهب إلى تبويب **Actions** في المستودع
2. ستجد workflow باسم "Deploy to GitHub Pages"
3. انتظر حتى يظهر ✅ (عادة 2-3 دقائق)

### 4.4 الوصول للموقع

بعد انتهاء البناء، سيكون موقعك متاحاً على:

```
https://YOUR_USERNAME.github.io/trading-analyzer-pwa/
```

---

## 🔄 الخطوة 5: تحديث الموقع

كلما أردت تحديث الموقع:

```bash
# 1. قم بالتعديلات المطلوبة
# 2. اختبر محلياً
npm run dev

# 3. Commit التعديلات
git add .
git commit -m "وصف التحديث"

# 4. رفع للـ GitHub
git push

# سيتم النشر تلقائياً!
```

---

## 🎨 الخطوة 6: تخصيص النطاق (اختياري)

### 6.1 استخدام نطاق مخصص

إذا كان لديك نطاق خاص:

1. في إعدادات Pages
2. قسم "Custom domain"
3. أدخل نطاقك: `trading.yourdomain.com`
4. أضف سجلات DNS:

```
Type: CNAME
Name: trading
Value: YOUR_USERNAME.github.io
```

### 6.2 تفعيل HTTPS

- GitHub Pages يوفر HTTPS مجاناً
- اختر "Enforce HTTPS" في الإعدادات

---

## 🔍 استكشاف الأخطاء الشائعة

### المشكلة 1: الصفحة فارغة أو 404

**الحل**:
```javascript
// في vite.config.js تأكد من:
base: '/trading-analyzer-pwa/',  // مع الشرطة المائلة في النهاية
```

### المشكلة 2: Build Failed

**الحل**:
```bash
# تأكد من أن البناء يعمل محلياً
npm run build

# إذا نجح محلياً، تحقق من:
# - إصدار Node.js في GitHub Actions (يجب 18+)
# - الاعتماديات في package.json
```

### المشكلة 3: الموارد لا تحمّل (CSS/JS)

**الحل**:
- تأكد من `base` في vite.config.js صحيح
- امسح الكاش: Ctrl+Shift+R

### المشكلة 4: Permission Denied

**الحل**:
```bash
# أنشئ Personal Access Token من GitHub
# Settings → Developer settings → Personal access tokens → Generate new token
# استخدمه بدلاً من كلمة المرور
```

---

## 📊 الخطوة 7: مراقبة الأداء

### 7.1 Google Analytics (اختياري)

أضف إلى `index.html`:

```html
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-XXXXXXXXXX');
</script>
```

### 7.2 مراقبة الأخطاء

يمكنك إضافة Sentry أو LogRocket لمراقبة الأخطاء.

---

## 🌟 نصائح للنجاح

### ✅ أفضل الممارسات

1. **اختبر محلياً دائماً** قبل الرفع
2. **استخدم Commits واضحة**
3. **راجع Actions** بعد كل push
4. **احتفظ بنسخة احتياطية** من مفاتيح API
5. **حدّث التوثيق** مع كل تغيير

### 🔐 الأمان

- **لا ترفع مفاتيح API** إلى GitHub
- استخدم `.env` و `.gitignore`
- اجعل المفاتيح في environment secrets

### ⚡ الأداء

- استخدم `npm run build` للتحقق من حجم الملفات
- فعّل Compression في GitHub Pages
- قلل حجم الصور

---

## 🎯 الخطوات التالية

بعد النشر بنجاح:

1. ✅ **شارك الرابط** مع الأصدقاء
2. ✅ **اجمع التغذية الراجعة**
3. ✅ **أضف ميزات جديدة**
4. ✅ **حسّن الأداء**
5. ✅ **ساهم في المشروع**

---

## 📱 النشر على خدمات أخرى (اختياري)

### Vercel
```bash
npm i -g vercel
vercel
```

### Netlify
```bash
npm i -g netlify-cli
netlify deploy
```

### Cloudflare Pages
1. اذهب لـ Cloudflare Pages
2. اربط مستودع GitHub
3. سيتم النشر تلقائياً

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. راجع [README.md](./README.md)
2. افتح Issue على GitHub
3. راجع [GitHub Pages Docs](https://docs.github.com/en/pages)

---

**تهانينا! 🎉 موقعك الآن حي على الإنترنت!**

شارك الرابط: `https://YOUR_USERNAME.github.io/trading-analyzer-pwa/`

---

التداول ينطوي على مخاطر - استخدم بحكمة! 💰
