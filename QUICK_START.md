# 🚀 دليل البدء السريع

## للتشغيل على جهازك المحلي (5 دقائق)

### 1. تثبيت Node.js
- قم بتحميل وتثبيت Node.js من: https://nodejs.org/
- اختر النسخة LTS (الموصى بها)

### 2. فتح Terminal/CMD

**Windows**: ابحث عن "cmd" أو "PowerShell"
**Mac/Linux**: افتح Terminal

### 3. التنقل للمشروع

```bash
cd trading-analyzer-pwa
```

### 4. تثبيت الاعتماديات

```bash
npm install
```

انتظر حتى ينتهي التحميل (2-3 دقائق)

### 5. تشغيل التطبيق

```bash
npm run dev
```

### 6. فتح المتصفح

افتح المتصفح واذهب إلى:
```
http://localhost:5173
```

🎉 **تم! التطبيق يعمل الآن!**

---

## للتشغيل على Termux (Android)

### 1. تثبيت Termux
- حمّل من F-Droid: https://f-droid.org/en/packages/com.termux/
- **لا تستخدم نسخة Google Play** (قديمة)

### 2. تحديث النظام

```bash
pkg update && pkg upgrade -y
```

### 3. تثبيت Node.js و Git

```bash
pkg install nodejs-lts git -y
```

### 4. استنساخ المشروع

إذا كان لديك المشروع على GitHub:
```bash
git clone https://github.com/YOUR_USERNAME/trading-analyzer-pwa.git
cd trading-analyzer-pwa
```

أو إذا كان المشروع في الذاكرة الداخلية:
```bash
# السماح بالوصول للتخزين
termux-setup-storage

# الانتقال للمجلد
cd storage/downloads/trading-analyzer-pwa
```

### 5. تثبيت الاعتماديات

```bash
npm install
```

### 6. تشغيل السيرفر

```bash
npm run dev -- --host 0.0.0.0 --port 5173
```

### 7. فتح التطبيق

ستظهر رسالة مثل:
```
➜  Local:   http://localhost:5173/
➜  Network: http://192.168.1.xxx:5173/
```

- افتح الرابط المحلي على نفس الجهاز
- أو افتح رابط Network من أي جهاز آخر على نفس الشبكة

---

## النشر على GitHub Pages

### الطريقة السريعة

1. **إنشاء حساب GitHub** (إذا لم يكن لديك):
   - اذهب إلى: https://github.com/signup

2. **إنشاء مستودع جديد**:
   - اضغط على "New repository"
   - اسم المستودع: `trading-analyzer-pwa`
   - اختر Public
   - اضغط "Create repository"

3. **رفع الملفات**:

```bash
# في مجلد المشروع
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/trading-analyzer-pwa.git
git push -u origin main
```

استبدل `YOUR_USERNAME` باسم المستخدم الخاص بك

4. **تفعيل GitHub Pages**:
   - اذهب لإعدادات المستودع (Settings)
   - اضغط على "Pages"
   - في "Source" اختر "GitHub Actions"
   - انتظر 2-3 دقائق

5. **فتح الموقع**:
```
https://YOUR_USERNAME.github.io/trading-analyzer-pwa/
```

---

## نصائح مهمة

### ✅ للحصول على أفضل أداء:

1. **استخدم Chrome أو Edge** (أفضل دعم للـ PWA)
2. **ثبّت التطبيق** على شاشة البداية (Add to Home Screen)
3. **فعّل الإشعارات** للحصول على تنبيهات التداول

### ⚠️ تنبيهات:

- **حدود API المجانية**:
  - Binance: بدون حدود
  - Alpha Vantage: 500 طلب/يوم
  - Twelve Data: 800 طلب/يوم

- **لا تشارك مفاتيح API الخاصة بك**

### 🔧 حل المشاكل الشائعة:

**المشكلة**: `npm: command not found`
**الحل**: تأكد من تثبيت Node.js بشكل صحيح

**المشكلة**: `EACCES: permission denied`
**الحل** (Linux/Mac):
```bash
sudo chown -R $USER:$USER .
```

**المشكلة**: البيانات لا تتحدث
**الحل**: 
1. تحقق من الاتصال بالإنترنت
2. تحقق من صحة مفاتيح API
3. تحقق من حدود الاستخدام

---

## الحصول على مفاتيح API

### Alpha Vantage (مجاني):
1. اذهب إلى: https://www.alphavantage.co/support/#api-key
2. أدخل بريدك الإلكتروني
3. ستصلك رسالة بالمفتاح
4. انسخ المفتاح في ملف `src/TradingAnalyzer.jsx`

### Twelve Data (مجاني):
1. اذهب إلى: https://twelvedata.com/
2. سجّل حساب جديد
3. اذهب إلى Dashboard
4. انسخ API Key
5. الصقه في ملف `src/TradingAnalyzer.jsx`

---

## الدعم والمساعدة

- **وثائق Vite**: https://vitejs.dev/
- **وثائق React**: https://react.dev/
- **دليل PWA**: https://web.dev/progressive-web-apps/

---

**صُنع بـ ❤️ للمتداولين العرب**

التداول ينطوي على مخاطر - استثمر بحكمة! 💰
